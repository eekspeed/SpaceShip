package Game;

public class ArmVulcan {
    // 是否启用
    public static boolean enable = false;

    // 武器等级
    public static int level = 0;
    
    public static void fire(int x, int y, Dir dir, GameFrame gf){
        if(level==0){
            
        }
    }
}
